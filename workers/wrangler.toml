name = "axion-tts-worker"
main = "src/index.js"
compatibility_date = "2025-04-01"

# Secrets (set via dashboard or CLI — never commit real keys):
#   wrangler secret put GEMINI_API_KEY
#
# Optional plain variables:
# [vars]
# ALLOWED_ORIGIN = "https://axion-neuralis.axn.cc.cd."
